from setuptools import find_packages, setup


setup(
    name="healthcare-dataflow-pipeline",
    version="1.0.0",
    description="Healthcare batch and streaming Dataflow pipelines",
    packages=find_packages(),
)

